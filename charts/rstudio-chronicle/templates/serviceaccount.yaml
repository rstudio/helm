{{- if $.Values.serviceaccount.enabled -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: chronicle-sa
  labels:
    app: chronicle-server
  annotations:
    {{ include "chronicle.serviceaccount.annotations" . | indent 4 }}
{{- end -}}
